# Railway otomatik enjekte eder
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hegemonia

# Zorunlu — production'da güçlü bir secret kullanın
JWT_SECRET=change-me-in-production-use-openssl-rand-hex-32
JWT_EXPIRES_IN=7d

PORT=3001
NODE_ENV=development

# Geliştirme: web Vite proxy için (production'da gerekmez)
CORS_ORIGIN=http://localhost:5173
